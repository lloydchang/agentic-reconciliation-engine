# AI Gateway Template - INCOMPLETE
# TODO: Complete this template for Claude Code proxying and security
# Use prompts like: "Create agentgateway deployment with prompt guards and audit logging"

apiVersion: apps/v1
kind: Deployment
metadata:
  name: claude-code-gateway
  namespace: control-plane
  labels:
    app: claude-gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: claude-gateway
  template:
    metadata:
      labels:
        app: claude-gateway
    spec:
      containers:
      - name: agentgateway
        # TODO: Replace with actual agentgateway image
        image: TODO_AGENTGATEWAY_IMAGE:latest
        ports:
        - containerPort: 8080
        env:
        # TODO: Add Anthropic API key
        - name: ANTHROPIC_API_KEY
          valueFrom:
            secretKeyRef:
              name: TODO_CLAUDE_SECRETS
              key: api-key
        # TODO: Configure prompt guards for sensitive data filtering
        # TODO: Enable audit logging
        # TODO: Set up network policies
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
---
# TODO: Add Service for gateway access
# TODO: Add Ingress/Gateway API configuration
# TODO: Add NetworkPolicy for traffic control
# TODO: Add RBAC for gateway management
