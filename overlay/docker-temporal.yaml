log-level: info
persistence:
  default:
    driver: postgres
    postgres:
      host: temporal-postgres
      port: 5432
      user: temporal
      password: temporal
      database: temporal
  visibility:
    driver: postgres
    postgres:
      host: temporal-postgres
      port: 5432
      user: temporal
      password: temporal
      database: temporal_visibility
