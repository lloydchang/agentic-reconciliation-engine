apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- deployment.yaml
- service.yaml
- configmap.yaml
- agent-skills-configmap.yaml

# Exclude servicemonitor.yaml as the CRD is missing
# - servicemonitor.yaml
