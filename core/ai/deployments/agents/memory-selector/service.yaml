apiVersion: v1
kind: Service
metadata:
  name: memory-selector-service
  namespace: ai-infrastructure
  labels:
    component: memory-selector
spec:
  selector:
    component: memory-selector
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  type: ClusterIP
