{
  "mcpServers": {
    "cloud-compliance": {
      "command": "node",
      "args": ["/Users/lloyd/github/antigravity/agentic-reconciliation-engine/.claude/mcp-servers/cloud-compliance/index.js"],
      "env": {
        "AWS_ACCESS_KEY_ID": "${AWS_ACCESS_KEY_ID}",
        "AWS_SECRET_ACCESS_KEY": "${AWS_SECRET_ACCESS_KEY}",
        "AWS_DEFAULT_REGION": "${AWS_DEFAULT_REGION:-us-east-1}",
        "AZURE_CLIENT_ID": "${AZURE_CLIENT_ID}",
        "AZURE_CLIENT_SECRET": "${AZURE_CLIENT_SECRET}",
        "AZURE_TENANT_ID": "${AZURE_TENANT_ID}",
        "AZURE_SUBSCRIPTION_ID": "${AZURE_SUBSCRIPTION_ID}",
        "GOOGLE_APPLICATION_CREDENTIALS": "${GOOGLE_APPLICATION_CREDENTIALS}",
        "GCP_PROJECT_ID": "${GCP_PROJECT_ID}",
        "GCP_ORGANIZATION_ID": "${GCP_ORGANIZATION_ID}"
      }
    },
    "incident-triage": {
      "command": "node",
      "args": ["/Users/lloyd/github/antigravity/agentic-reconciliation-engine/.claude/mcp-servers/incident-triage/index.js"],
      "env": {
        "PROMETHEUS_URL": "${PROMETHEUS_URL}",
        "DATADOG_API_KEY": "${DATADOG_API_KEY}",
        "DATADOG_APP_KEY": "${DATADOG_APP_KEY}",
        "PAGERDUTY_API_KEY": "${PAGERDUTY_API_KEY}",
        "SLACK_BOT_TOKEN": "${SLACK_BOT_TOKEN}",
        "SLACK_SIGNING_SECRET": "${SLACK_SIGNING_SECRET}",
        "SMTP_HOST": "${SMTP_HOST}",
        "SMTP_PORT": "${SMTP_PORT:-587}",
        "SMTP_USERNAME": "${SMTP_USERNAME}",
        "SMTP_PASSWORD": "${SMTP_PASSWORD}",
        "INCIDENT_SYSTEM_URL": "${INCIDENT_SYSTEM_URL}",
        "INCIDENT_API_KEY": "${INCIDENT_API_KEY}"
      }
    },
    "iac-validator": {
      "command": "node",
      "args": ["/Users/lloyd/github/antigravity/agentic-reconciliation-engine/.claude/mcp-servers/iac-validator/index.js"],
      "env": {
        "AWS_ACCESS_KEY_ID": "${AWS_ACCESS_KEY_ID}",
        "AWS_SECRET_ACCESS_KEY": "${AWS_SECRET_ACCESS_KEY}",
        "AWS_DEFAULT_REGION": "${AWS_DEFAULT_REGION:-us-east-1}",
        "AZURE_CLIENT_ID": "${AZURE_CLIENT_ID}",
        "AZURE_CLIENT_SECRET": "${AZURE_CLIENT_SECRET}",
        "AZURE_TENANT_ID": "${AZURE_TENANT_ID}",
        "AZURE_SUBSCRIPTION_ID": "${AZURE_SUBSCRIPTION_ID}",
        "GOOGLE_APPLICATION_CREDENTIALS": "${GOOGLE_APPLICATION_CREDENTIALS}",
        "GCP_PROJECT_ID": "${GCP_PROJECT_ID}",
        "INFRACOST_API_KEY": "${INFRACOST_API_KEY}",
        "PRICING_API_KEY": "${PRICING_API_KEY}"
      }
    },
    "knowledge-base": {
      "command": "node",
      "args": ["/Users/lloyd/github/antigravity/agentic-reconciliation-engine/.claude/mcp-servers/knowledge-base/index.js"],
      "env": {
        "VECTOR_DB_URL": "${VECTOR_DB_URL}",
        "VECTOR_DB_API_KEY": "${VECTOR_DB_API_KEY}",
        "VECTOR_DB_INDEX_NAME": "${VECTOR_DB_INDEX_NAME:-knowledge_base}",
        "POSTGRES_URL": "${POSTGRES_URL}",
        "POSTGRES_HOST": "${POSTGRES_HOST:-localhost}",
        "POSTGRES_PORT": "${POSTGRES_PORT:-5432}",
        "POSTGRES_DB": "${POSTGRES_DB:-knowledge_base}",
        "POSTGRES_USER": "${POSTGRES_USER}",
        "POSTGRES_PASSWORD": "${POSTGRES_PASSWORD}",
        "REDIS_URL": "${REDIS_URL:-redis://localhost:6379}",
        "REDIS_HOST": "${REDIS_HOST:-localhost}",
        "REDIS_PORT": "${REDIS_PORT:-6379}",
        "REDIS_PASSWORD": "${REDIS_PASSWORD}",
        "KNOWLEDGE_BASE_PATH": "${KNOWLEDGE_BASE_PATH:-/data/knowledge-base}",
        "EMBEDDING_MODEL": "${EMBEDDING_MODEL:-text-embedding-ada-002}",
        "OPENAI_API_KEY": "${OPENAI_API_KEY}"
      }
    },
    "engagement-sync": {
      "command": "node",
      "args": ["/Users/lloyd/github/antigravity/agentic-reconciliation-engine/.claude/mcp-servers/engagement-sync/index.js"],
      "env": {
        "SLACK_BOT_TOKEN": "${SLACK_BOT_TOKEN}",
        "SLACK_SIGNING_SECRET": "${SLACK_SIGNING_SECRET}",
        "TEAMS_WEBHOOK_URL": "${TEAMS_WEBHOOK_URL}",
        "TEAMS_CLIENT_ID": "${TEAMS_CLIENT_ID}",
        "TEAMS_CLIENT_SECRET": "${TEAMS_CLIENT_SECRET}",
        "TEAMS_TENANT_ID": "${TEAMS_TENANT_ID}",
        "SMTP_HOST": "${SMTP_HOST}",
        "SMTP_PORT": "${SMTP_PORT:-587}",
        "SMTP_USERNAME": "${SMTP_USERNAME}",
        "SMTP_PASSWORD": "${SMTP_PASSWORD}",
        "JIRA_URL": "${JIRA_URL}",
        "JIRA_USERNAME": "${JIRA_USERNAME}",
        "JIRA_API_TOKEN": "${JIRA_API_TOKEN}",
        "ASANA_API_KEY": "${ASANA_API_KEY}",
        "ASANA_WORKSPACE_ID": "${ASANA_WORKSPACE_ID}",
        "GITHUB_TOKEN": "${GITHUB_TOKEN}"
      }
    }
  },
  "toolChoice": "auto",
  "timeout": 120,
  "maxTokens": 8192,
  "temperature": 0.7,
  "systemPrompt": "You are an AI assistant with access to specialized MCP servers for cloud compliance auditing, incident triage, IaC validation, knowledge management, and stakeholder engagement. Use these tools to provide comprehensive assistance with Agentic Reconciliation Engine management, security compliance, incident response, and organizational knowledge sharing. Always consider the context and provide actionable insights based on the available tools and data."
}
