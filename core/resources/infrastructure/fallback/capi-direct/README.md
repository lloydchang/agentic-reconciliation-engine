# Direct CAPI Cluster Manifests (Deprecated)

These direct `Cluster` resources were used before `XCluster` claims were wired
to generate CAPI objects via Crossplane. They are kept for reference only and
are not included in active kustomizations.
