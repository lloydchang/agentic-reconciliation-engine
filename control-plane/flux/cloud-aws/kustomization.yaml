resources:
  - ../../infrastructure/tenants/aws/network
  - ../../infrastructure/tenants/aws/clusters
  - ../../infrastructure/tenants/aws/workloads
patchesStrategicMerge:
  - patches/vpc-overrides.yaml
  - patches/eks-overrides.yaml
