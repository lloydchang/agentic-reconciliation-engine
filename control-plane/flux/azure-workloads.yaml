apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: azure-workloads
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./infrastructure/tenants/azure/workloads
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
  - name: azure-clusters
