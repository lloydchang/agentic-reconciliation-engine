apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: gcp-workloads
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./infrastructure/tenants/gcp/workloads
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
  - name: gcp-clusters
