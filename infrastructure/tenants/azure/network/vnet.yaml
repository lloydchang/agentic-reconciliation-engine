apiVersion: network.azure.com/v1beta20201101
kind: VirtualNetwork
metadata:
  name: main-vnet
spec:
  location: ${LOCATION}
  owner:
    name: ${RESOURCE_GROUP}
  addressSpace:
    addressPrefixes:
    - 10.0.0.0/16
  subnets:
  - name: default
    properties:
      addressPrefix: 10.0.0.0/24
