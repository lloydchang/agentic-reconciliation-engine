apiVersion: v1
kind: Service
metadata:
  name: nginx-sample
  namespace: default
  labels:
    app: nginx-sample
spec:
  selector:
    app: nginx-sample
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: LoadBalancer
