# falco - Enterprise tool implementation
apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: HelmRepository
metadata:
  name: falco
  namespace: default
spec:
  interval: 10m0s
  url: https://charts.example.com  # Replace with actual Helm repo URL
---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: falco
  namespace: default
spec:
  interval: 10m0s
  chart:
    spec:
      chart: falco
      version: "1.0.0"
      sourceRef:
        kind: HelmRepository
        name: falco
      interval: 10m0s
