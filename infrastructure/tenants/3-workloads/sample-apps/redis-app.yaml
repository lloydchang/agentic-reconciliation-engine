---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-sample
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis-sample
  template:
    metadata:
      labels:
        app: redis-sample
    spec:
      containers:
      - name: redis
        image: redis:7.2
        ports:
        - containerPort: 6379
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: redis-sample-service
  namespace: default
spec:
  selector:
    app: redis-sample
  ports:
  - protocol: TCP
    port: 6379
    targetPort: 6379
  type: ClusterIP
