# Raw Provider Manifests (Deprecated)

These manifests used direct ACK/ASO/KCC CRDs for networking and clusters.
They are retained for reference only and are not included in active Kustomizations.

Use Crossplane XRD claims under `infrastructure/tenants/` instead.
