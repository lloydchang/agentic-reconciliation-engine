apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: gitops-infra-control-plane-root
  namespace: flux-system
resources:
  - control-plane/
  - infrastructure/
labels:
  - pairs:
      managed-by: flux
      component: gitops-infra-control-plane
      platform: multi-cluster-gitops
