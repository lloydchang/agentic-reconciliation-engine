# AWS ACK Controllers Template - INCOMPLETE
# TODO: Complete this template with actual ACK controller deployments
# Use prompts like: "Create AWS ACK controllers for EC2, EKS, VPC, etc."

apiVersion: apps/v1
kind: Deployment
metadata:
  name: ack-ec2-controller
  namespace: ack-system
  labels:
    app: ack-controller
    service: ec2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ack-controller
      service: ec2
  template:
    metadata:
      labels:
        app: ack-controller
        service: ec2
    spec:
      containers:
      - name: controller
        # TODO: Replace with actual ACK EC2 controller image
        image: TODO_ACK_EC2_IMAGE:latest
        env:
        # TODO: Add AWS credentials and region
        - name: AWS_REGION
          value: TODO_AWS_REGION
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
      # TODO: Add service account for IRSA
      serviceAccountName: ack-ec2-controller
---
# TODO: Add ACK EKS controller
# TODO: Add ACK VPC controller
# TODO: Add ACK RDS controller
# TODO: Add RBAC policies
# TODO: Add CustomResourceDefinitions
